# using the api keys generated to make routes for the webapp
# start by making all the required imports
from flask import render_template
# import the blueprint application from this very same folder
from . import main
